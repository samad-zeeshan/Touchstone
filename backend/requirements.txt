# Runtime dependencies, pinned to the tested versions.
# openai pulls in httpx; python-dotenv is only used for local .env loading.
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
openai==2.43.0
python-dotenv==1.2.2
